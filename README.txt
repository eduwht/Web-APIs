Perform the following actions to be able to make API requests in your browser:

1. Allow CORS (Cross-Origin Resource Sharing) requests in your browser.
TIP: You can install "Moesif Orign & CORS Changer" extension in Chrome Browser.

2. Open index.html file and test it. It should be working after enabling CORS.